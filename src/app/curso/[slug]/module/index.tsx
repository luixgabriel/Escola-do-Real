// Pegar todos os módulos através de uma requisição

export default async function Module({ id }: { id: number }) {
  // Encontrar o módulo atual através do id
  // Criar o sistema de sanfona
  // Inserir a lista de aulas
  return <p>Módulo {id}</p>
}
